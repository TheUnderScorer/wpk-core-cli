const $ = jQuery.noConflict();
export default $;

export const $window = $( window );

export const $document = $( document );

export const $body = $( 'body' );

export const $datepickers = $( '.datepicker:visible' );

export const $main = $( '.main' );
